name: Url-Shortener Image Build workflow
on:
  push:
    branches:
      - develop
      - staging
    tags:
      - 'v*'
    paths:
      - 'backend/core-services/egov-url-shortening/**'
  workflow_dispatch:

permissions:
  security-events: write
  contents: read

jobs:
  Docker_Image_Build:
    if: (github.event_name == 'push' && (startsWith(github.ref, 'refs/heads/develop') || startsWith(github.ref, 'refs/heads/staging') || startsWith(github.ref, 'refs/tags/v'))) || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Docker Buildx  # Buildx is docker CLI plugin which uses BuildKit capabilities
        uses: docker/setup-buildx-action@v3   # Buildkit is the underling engine that handles the actual building of docker images.

      - name: Set Version from Tag or Branch
        id: set_version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}  # Use tag name for version
          elif [[ "${GITHUB_REF}" == refs/heads/* ]]; then
            # Use branch name as version, replacing '/' with '-'
            VERSION=${GITHUB_REF#refs/heads/}
            VERSION=$(echo "${VERSION}" | sed 's|/|-|g')
          else
            VERSION="latest"  # Default version if not a tag or branch
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV

      - name: Git commit_hash for service
        run: |
          commit_hash=$(git log -1 --pretty=format:%H -- backend/core-services/egov-url-shortening)
          echo "commit_hash=$commit_hash" >> $GITHUB_ENV

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Cache Maven dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2/repository
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      # Login to Docker Container Registry
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build Jar file
        run: mvn -B -f backend/core-services/egov-url-shortening/pom.xml package

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: build/maven/Dockerfile
          push: true
          tags: selcohub/egov-url-shortener:${{ env.VERSION }}-${{ env.commit_hash }}
          cache-from: type=gha   # Stores cache in GitHub Actions .
          cache-to: type=gha,mode=max  # max- stores all the layers including the intermediate layers in multi-stage
          build-args: |
            JAR_FILE=backend/core-services/egov-url-shortening/target/*.jar

      - name: Build and Push Docker image for egov-url-shortener-Db
        run: |
          docker build  -t selcohub/egov-url-shortener-db:${{ env.VERSION }}-${{ env.commit_hash }} -f  Dockerfile .
          docker push selcohub/egov-url-shortener-db:${{ env.VERSION }}-${{ env.commit_hash }}
        working-directory: backend/core-services/egov-url-shortening/src/main/resources/db
