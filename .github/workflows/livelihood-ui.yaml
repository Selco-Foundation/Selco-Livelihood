name: livelihood-ui Image Build workflow
on:
  push:
    branches:
      - develop
      - staging
    tags:
      - 'v*'
    paths:
      - 'frontend/livelihood-ui/**'
  workflow_dispatch:

permissions:
  security-events: write
  contents: read

jobs:
  Docker_Image_Build:
    if: (github.event_name == 'push' && (startsWith(github.ref, 'refs/heads/develop') || startsWith(github.ref, 'refs/heads/staging') || startsWith(github.ref, 'refs/tags/v'))) || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Docker Buildx  # Buildx is docker CLI plugin which uses BuildKit capabilities
        uses: docker/setup-buildx-action@v3   # Buildkit is the underling engine that handles the actual building of docker images.

      # Login to Docker Container Registry
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      - name: Set Version from Tag or Branch
        id: set_version
        run: |
          if [[ "${GITHUB_REF}" == refs/tags/* ]]; then
            VERSION=${GITHUB_REF#refs/tags/}  # Use tag name for version
          elif [[ "${GITHUB_REF}" == refs/heads/* ]]; then
            # Use branch name as version, replacing '/' with '-'
            VERSION=${GITHUB_REF#refs/heads/}
            VERSION=$(echo "${VERSION}" | sed 's|/|-|g')
          else
            VERSION="latest"  # Default version if not a tag or branch
          fi
          echo "VERSION=$VERSION" >> $GITHUB_ENV
      - name: Git commit_hash for service
        run: |
          commit_hash=$(git log -1 --pretty=format:%H -- frontend/livelihood-ui)
          echo "commit_hash=$commit_hash" >> $GITHUB_ENV

      - name: Set GA Measurement ID
        run: |
          if [[ "${GITHUB_REF}" == "refs/heads/develop" ]]; then
            echo "GA_MEASUREMENT_ID=${{ vars.DEV_MEASUREMENT_ID }}" >> $GITHUB_ENV
          elif [[ "${GITHUB_REF}" == "refs/heads/staging" ]]; then
            echo "GA_MEASUREMENT_ID=${{ vars.STAGING_MEASUREMENT_ID }}" >> $GITHUB_ENV
          elif [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            echo "GA_MEASUREMENT_ID=${{ vars.PROD_MEASUREMENT_ID }}" >> $GITHUB_ENV
          fi

        # Builds & Push Docker images using buildx cli plugin of docker buildx builder
      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: frontend/livelihood-ui/docker/Dockerfile
          push: true
          tags: selcohub/livelihood-ui:${{ env.VERSION }}-${{ env.commit_hash }}
          cache-from: type=gha   # Stores cache in GitHub Actions .
          cache-to: type=gha,mode=max  # max- stores all the layers including the intermediate layers in multi-stage
          build-args: |
            WORK_DIR=frontend/livelihood-ui/
            GA_MEASUREMENT_ID=${{ env.GA_MEASUREMENT_ID }}

  # Workflow_Trigger:
  #   if: startsWith(github.ref, 'refs/heads/develop')
  #   runs-on: ubuntu-latest
  #   needs: Docker_Image_Build
  #   steps:
  #     - name: Trigger Deployment Pipeline
  #       run: |
  #         curl -X POST \
  #         -H "Accept: application/vnd.github.v3+json" \
  #         -H "Authorization: token ${{ secrets.GHUB_TOKEN }}" \
  #         https://api.github.com/repos/Selco-Foundation/DIGIT-DevOps/actions/workflows/Dev.yaml/dispatches \
  #         -d '{"ref":"Selco-Dev"}'
