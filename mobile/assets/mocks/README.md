# Temporary activity payload fixtures

`solar_component_defaults.json` supplies missing Solar component values at
the activity-facility decoding boundary. Backend values take precedence.
Deleting that JSON file disables the fallback without requiring Dart changes.
